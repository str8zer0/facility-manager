[project]
name = "facility-manager"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "django>=6.0.3",
    "django-environ>=0.13.0",
    "psycopg>=3.3.3",
]
