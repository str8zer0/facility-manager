[phases.setup]
nixPkgs = ["python313", "uv", "postgresql"]

[phases.install]
cmds = [
    "uv sync --no-dev"
]

[phases.build]
cmds = [
    "python manage.py collectstatic --noinput",
    "python manage.py migrate --noinput"
]

[start]
cmd = "gunicorn facility_manager.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --threads 2 --timeout 120"